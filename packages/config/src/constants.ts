export const ESM_FOLDER = 'esm';

export const CJS_FOLDER = 'lib';

export const MIN_IE_VERSION = 11;

export const DIR_PATTERN = '{bin,scripts,src,tests}';

export const MIN_NODE_VERSION = '10.16.0';

export const IGNORE_PATHS = [
  'coverage/',
  'node_modules/',
  'public/',
  'esm/',
  'lib/',
  'tmp/',
  'dist/',
  '*.d.ts',
];

export const EXTS = ['.ts', '.tsx', '.js', '.jsx', '.json']

export const ASSET_EXT_PATTERN = /\.(ttf|eot|otf|svg|woff|woff2|mp3|png|jpg|jpeg|gif|ico)$/;
